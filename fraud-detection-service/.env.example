# TRUST Fraud Detection Service — local overrides
# Copy to `.env` when running outside Docker Compose.

# Trust-score service for behavioral consistency signals
TRUST_SCORE_SERVICE_URL=http://localhost:8003
